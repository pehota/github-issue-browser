overwrite: true
schema: "./schema.json"
documents: "**/*.graphql"
generates:
  src/types/generated.ts:
    plugins:
      - "typescript-common"
  ./graphql.schema.json:
    plugins:
      - "introspection"
